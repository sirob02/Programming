#!/bin/bash
options="Run Exit"
select option in $options; do
	if [ "Run" = $option ] ; then
		clear
		echo "Dein Programm startet..."
		exit
	elif [ "Exit" = $option ] ; then
		clear
		echo "Dein Program stoppt..." 
		exit
	else 
		echo error
	fi
done
